package io.github.stcksmsh.pravnik.data.db.migrations

import androidx.room.migration.Migration
import androidx.sqlite.db.SupportSQLiteDatabase

/**
 * Room migrations for AppDb.
 * Version 1 is the initial version. Add migrations here when bumping the DB version.
 */
object AppDbMigrations {
    // Example placeholder for future migration from 1 to 2
    // val MIGRATION_1_2 = object : Migration(1, 2) {
    //     override fun migrate(db: SupportSQLiteDatabase) {
    //         // Perform ALTER TABLE / CREATE INDEX etc.
    //     }
    // }

    val ALL: Array<Migration> = arrayOf(
        // Add future migrations here, e.g., MIGRATION_1_2
    )
}

